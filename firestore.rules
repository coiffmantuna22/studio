
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // -------------------------------------------------------------------------
    // Helper Functions
    // -------------------------------------------------------------------------

    /**
     * Checks if the user is authenticated.
     */
    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * Checks if the authenticated user's UID matches the provided userId.
     * This is the primary ownership check.
     */
    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    /**
     * Checks if the incoming resource data contains the correct owner userId.
     */
    function isResourceOwner(userId) {
      return request.resource.data.userId == userId;
    }

    // -------------------------------------------------------------------------
    // Collection Rules
    // -------------------------------------------------------------------------

    match /teachers/{teacherId} {
      allow read, create, update, delete: if isSignedIn() && isOwner(request.auth.uid) && isResourceOwner(request.auth.uid);
      allow list: if isSignedIn();
    }
    
    match /classes/{classId} {
      allow read, create, update, delete: if isSignedIn() && isOwner(request.auth.uid) && isResourceOwner(request.auth.uid);
      allow list: if isSignedIn();
    }
    
    match /settings/{settingId} {
      allow read, create, update, delete: if isSignedIn() && isOwner(request.auth.uid) && isResourceOwner(request.auth.uid);
      allow list: if isSignedIn();
    }
  }
}
